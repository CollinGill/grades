import React from "react";
import { Box, Center, Text } from "@chakra-ui/react";

const AppDesc = () => {
  return (
    <Box>
      <Center>
        <Text fontSize="24px">
          grades is a web app that's used to keep track of your grades in a
          class and calculate your GPA
        </Text>
      </Center>
    </Box>
  );
};

export default AppDesc;
